m4_divert(0)dnl
# Setup

    - CM.make "test.cm";
    ...
    val it = true : bool
    - fun parse fileName = UXML.parseRaw TextIO.StreamIO.input1 ((TextIO.getInstream o TextIO.openIn) fileName);
    ...
    val parse = fn : string -> ?.Parse.Ast.document list

m4_foreach([file], m4_split(m4_normalize(m4_esyscmd(ls xml-test-suite/xmlconf/ibm/valid/P*/*.xml))), [
m4_bmatch(file, [P02/ibm02v01.xml], [# P02 is skipped
], [[#] file

    - length (parse "file");
    val it = 1 : int
])])
